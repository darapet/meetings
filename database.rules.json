{
  "rules": {
    "presence": {
      "$meetingId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },
    "signals": {
      "$meetingId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },
    "rooms": {
      "$meetingId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },
    "joinRequests": {
      "$meetingId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
